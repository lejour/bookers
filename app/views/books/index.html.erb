<h1>Books</h1>
<% @books.each do |book| %>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <tb></tb>
      <tb></tb>
    </tr>
  </tbody>
</table>

<span><%= book.title %></span>
<span><%= book.body %></span>
<% end %>

<h2>New book</h2>

<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <h4>
  <%= f.submit 'Create Book' %>
  </h4>
<% end %>
